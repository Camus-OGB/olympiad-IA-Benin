/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'img.freepik.com', 'upload.wikimedia.org', 'semecity.bj', 'ioai-official.org'],
  },
};

module.exports = nextConfig;
